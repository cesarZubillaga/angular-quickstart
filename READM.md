#For VM shared folder installation
The installation of this files has fixed the main problem with the Virtual Machine
```
sudo npm install -g tsc
sudo npm install -g concurrently \
sudo npm install -g typescript \
````    
